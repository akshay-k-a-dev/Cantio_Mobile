<resources>
    <!-- Base application theme — Activity is transparent so Compose draws everything -->
    <style name="Theme.AppPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.AppPlayer.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0A0A0F</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_music_note</item>
        <item name="postSplashScreenTheme">@style/Theme.AppPlayer</item>
    </style>
</resources>
